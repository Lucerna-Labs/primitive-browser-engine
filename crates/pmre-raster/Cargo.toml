[package]
name = "pmre-raster"
version = "0.3.0"
edition = "2021"
license = "MIT"
description = "Raster, path, bloom, and post-processing primitives for PMRE."

[dependencies]
pmre-core = { path = "../pmre-core" }
