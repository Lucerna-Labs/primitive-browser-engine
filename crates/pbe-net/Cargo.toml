[package]
name = "pbe-net"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Network on-ramp facade — delegates to the modular pbe-proto protocol layer (http/ws/data, one crate each). Preserves the original fetch/fetch_bytes API for existing callers."

[dependencies]
cap-http = { workspace = true }
pbe-proto = { workspace = true }
