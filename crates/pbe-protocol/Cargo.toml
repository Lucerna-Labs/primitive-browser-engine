[package]
name = "pbe-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Typed bus contracts for the primitive browser engine — the message vocabulary stages route over the Spiderweb fabric."

[dependencies]
cap-html-parse = { workspace = true }
cap-style-cascade = { workspace = true }
cap-primitives = { workspace = true }
